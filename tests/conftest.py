"""pytest hook module. Shared, importable-by-name helpers live in `support.py`
instead -- `conftest` is a pytest-reserved filename, so importing it by name
elsewhere is a misuse.
"""

from __future__ import annotations
